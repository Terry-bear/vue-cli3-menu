import * as actions from '@/store-common/actions'
import * as getters from '@/store-common/getters'
import state from '@/store-common/state'
import mutations from '@/store-common/mutations'

export default {
  actions,
  getters,
  state,
  mutations
}
