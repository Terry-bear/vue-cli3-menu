export const leftWith = state => state.leftWith

export const isCollapse = state => state.leftWith !== 256

export const mainContentUrl = state => state.mainContentUrl

export const currentMenuIndex = state => state.currentMenuIndex

export const isFullscreen = state => state.isFullscreen
